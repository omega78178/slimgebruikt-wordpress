<?php
/**
 * ACF Field Groups – SlimGebruikt
 *
 * Veldgroepen staan in de ACF UI (acf-json folder).
 * Ga naar Custom Fields > Field Groups om ze te bewerken.
 * JSON-bestanden in /acf-json worden automatisch geladen en gesynchroniseerd.
 *
 * @package slimgebruikt
 */

// Velden worden geladen via acf-json/*.json (Local JSON)
// Geen PHP-registraties meer – bewerk in ACF > Field Groups
